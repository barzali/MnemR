<?xml version="1.0" encoding="utf-8"?>
<searchable xmlns:android="http://schemas.android.com/apk/res/android"
	android:hint="@string/app_name"
        android:label="@string/app_name"
        android:searchButtonText="Such"
        android:searchSuggestThreshold="0"
        android:queryAfterZeroResults="true"
        android:includeInGlobalSearch="true"
        android:searchMode="queryRewriteFromText"
        android:searchSuggestAuthority="com.mnemr"
        android:searchSettingsDescription="@string/app_name"
        android:searchSuggestIntentData="content://com.mnemr/mnemons">
</searchable>
